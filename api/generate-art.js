const RESPONSES_URL = "https://api.openai.com/v1/responses";
function prompt(data,label,instruction){const map=(data.semanticMap||[]).map((l,i)=>`${i+1}. ${l.type}: x ${Math.round(l.x||0)}%, y ${Math.round(l.y||0)}%, w ${Math.round(l.w||0)}%, h ${Math.round(l.h||0)}%`).join("\n");return `Você é diretor de arte. Crie SOMENTE A BASE VISUAL de um cartaz profissional de igreja a partir da imagem de referência.\n\nNÃO DESENHE PESSOAS. NÃO ESCREVA TEXTO. NÃO DESENHE LOGOS. NÃO ESCREVA MARCAS INTERNAS COMO \"Logo 1\", \"reserva\", \"pregador\", \"título\" ou qualquer rótulo de configuração.\n\nO aplicativo aplicará depois, de forma exata, pregador, logo, textos e fontes. Portanto crie fundos, texturas, luz, profundidade, grafismos e espaços adequados nas áreas mapeadas.\n\nSe hasLogo=${!!data?.church?.hasLogo}, NÃO escreva o nome da igreja em hipótese alguma.\n\nMAPA DE ÁREAS RESERVADAS:\n${map}\n\nELEMENTOS VISUAIS DESEJADOS: ${(data.visualElements||[]).join(', ')||'nenhum específico'}\nDIREÇÃO GERAL: ${data.instruction||'nenhuma'}\nVARIAÇÃO ${label}: ${instruction}\n\nA saída deve ser somente a base gráfica final sem textos, sem pessoas e sem logos.`}
function inputContent(data,text){const c=[{type:'input_text',text}];if(data.reference&&data.reference.startsWith('data:image/'))c.push({type:'input_image',image_url:data.reference,detail:'high'});return c}
async function gen(data,label,instruction){const size=data.format==='wide'?'1536x1024':'1024x1536';const r=await fetch(RESPONSES_URL,{method:'POST',headers:{Authorization:`Bearer ${process.env.OPENAI_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({model:'gpt-5-mini',input:[{role:'user',content:inputContent(data,prompt(data,label,instruction))}],tools:[{type:'image_generation',model:'gpt-image-1',action:'edit',input_fidelity:'high',quality:'medium',size,output_format:'png'}],tool_choice:'required'})});const d=await r.json();if(!r.ok)throw new Error(d?.error?.message||`Erro OpenAI ${r.status}`);const call=(d.output||[]).find(x=>x.type==='image_generation_call'&&x.result);if(!call?.result)throw new Error('A OpenAI não retornou a base visual.');return{label,dataUrl:`data:image/png;base64,${call.result}`}}
module.exports=async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Método não permitido.'});if(!process.env.OPENAI_API_KEY)return res.status(500).json({error:'OPENAI_API_KEY não configurada.'});try{const data=req.body||{};if(!data.reference)return res.status(400).json({error:'Envie a arte de referência.'});const v=data.variantInstructions||{};const specs=[['Fiel à referência',v.faithful||'Preserve rigorosamente composição e linguagem.'],['Equilibrada',v.balanced||'Preserve o DNA, melhorando equilíbrio.'],['Mais criativa',v.creative||'Maior liberdade visual, preservando áreas reservadas.']];const images=await Promise.all(specs.map(x=>gen(data,x[0],x[1])));return res.status(200).json({success:true,images});}catch(e){console.error(e);return res.status(500).json({error:e.message||'Erro ao gerar.'});}};
